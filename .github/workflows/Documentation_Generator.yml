name: Documentation Generator

on:
    push:
        branches: ["main"]
jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            - run: pip install -U sphinx
            - run: chmod u+x sphinx-build -b html . /Documentation
            - run: git add -A
